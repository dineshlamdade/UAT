import { TestBed } from '@angular/core/testing';

import { InvestmentIntereston80TTATTBMasterApprovalService } from './investment-intereston80-tta-ttb-master-approval.service';

describe('InvestmentIntereston80TTATTBMasterApprovalService', () => {
  let service: InvestmentIntereston80TTATTBMasterApprovalService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(InvestmentIntereston80TTATTBMasterApprovalService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
